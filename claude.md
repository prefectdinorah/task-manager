# Claude Code - Универсальные инструкции

## 🔄 Git Workflow (СТРОГО!)

### Ветки
- \`master\` - продакшн (мердж ТОЛЬКО вручную владельцем)
- \`dev\` - разработка (автомердж после тестов)
- \`feature/*\` - фичи (формат: \`feature/краткое-описание\`)

### Процесс работы

#### 1 ПЛАНИРОВАНИЕ (ОБЯЗАТЕЛЬНО!)

1. Создать спецификацию фичи в \`docs/features/\`
2. Отправить уведомление через \`npm run notify\`
3. **ЖДАТЬ ПОДТВЕРЖДЕНИЯ**

**Формат спецификации:**
\`\`\`markdown
# Фича: [Название]

## Описание
Что делает фича и зачем нужна.

## Требования
- [ ] Функциональное требование 1
- [ ] Функциональное требование 2

## Технические детали
- Компоненты: список
- API endpoints: список
- DB schema: изменения если есть

## Тестирование
- [ ] Unit тесты
- [ ] Integration тесты
- [ ] E2E сценарии

## Timeline
- Разработка: X часов
- Тестирование: Y часов
\`\`\`

#### 2 РАЗРАБОТКА

\`\`\`bash
# 1. Создать ветку от dev
git checkout dev
git pull origin dev
git checkout -b feature/название-фичи

# 2. Разработка согласно спецификации

# 3. Коммиты (conventional commits)
git commit -m "feat: описание изменения"
git commit -m "fix: описание фикса"

# 4. Уведомление
npm run notify "✅ Разработка завершена" "Детали..."

# 5. ЖДАТЬ ПОДТВЕРЖДЕНИЯ
\`\`\`

#### 3 ТЕСТИРОВАНИЕ

\`\`\`bash
# 1. Запуск тестов
npm run test
npm run test:e2e

# 2. Ручная проверка
npm run dev

# 3. Уведомление
npm run notify "🧪 Тестирование завершено" "Детали..."

# 4. ЖДАТЬ ПОДТВЕРЖДЕНИЯ
\`\`\`

#### 4 МЕРДЖ В DEV

\`\`\`bash
# Автоматический мердж + обновление документации
npm run merge:dev

# Скрипт делает:
# - Переключается на dev
# - Мерджит feature ветку
# - Обновляет документацию
# - Пушит в origin
# - Удаляет feature ветку
# - Отправляет уведомление
\`\`\`

## 📢 Telegram уведомления

### Когда отправлять:
1. ✅ После создания спецификации
2. ✅ После завершения разработки
3. ✅ После завершения тестирования
4. ✅ После мерджа в dev
5. При критических проблемах

### Использование:
\`\`\`bash
npm run notify "Заголовок" "Детали сообщения"
\`\`\`

### Эмодзи для статусов:
- 📋 Планирование
- 🔨 Разработка
- ✅ Завершено
- 🧪 Тестирование
- 🎉 Успех
- ⚠��� Внимание
- ❌ Ошибка

## 📚 Документация (ОБЯЗАТЕЛЬНО!)

### Структура docs/
- \`project_spec.md\` - описание проекта
- \`architecture.md\` - архитектура
- \`changelog.md\` - история изменений
- \`project_status.md\` - текущий статус
- \`features/\` - спецификации фич

### После каждой фичи обновлять:
1. **changelog.md** - добавить запись
2. **project_status.md** - обновить статус
3. **architecture.md** - если изменилась архитектура

### Команда:
\`\`\`bash
npm run docs:update
\`\`\`

## 🔧 Кастомные команды

\`\`\`json
{
  "scripts": {
    "merge:dev": "node .claude/scripts/merge-to-dev.js",
    "docs:update": "node .claude/scripts/update-docs.js",
    "status": "node .claude/scripts/check-status.js",
    "notify": "node .claude/scripts/notify.js"
  }
}
\`\`\`

## ВАЖНЫЕ ПРАВИЛА

### ❌ НИКОГДА:
- НЕ начинай разработку без подтверждённой спецификации
- НЕ мерджи в master (только владелец)
- НЕ пропускай тестирование
- НЕ забывай обновлять документацию
- НЕ делай важные шаги без уведомлений

### ✅ ВСЕГДА:
- Жди подтверждения перед переходом к следующему этапу
- Отправляй понятные уведомления
- Следуй стандартам кода проекта (см. PROJECT.md)
- Обновляй документацию
- Пиши чистый код
