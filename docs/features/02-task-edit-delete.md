# Фича: Редактирование и удаление задач

## Описание
Добавление возможности редактировать и удалять существующие задачи. Пользователи смогут:
- Редактировать название и описание задачи
- Удалять ненужные задачи
- Отменять изменения при редактировании

## Требования

### Функциональные требования
- [ ] Кнопка "Редактировать" на каждой задаче
- [ ] Кнопка "Удалить" на каждой задаче
- [ ] Модальное окно/inline форма для редактирования
- [ ] Подтверждение при удалении задачи
- [ ] Возможность отменить редактирование
- [ ] Сохранение изменений в базу данных
- [ ] Обновление UI после изменений
- [ ] Обработка ошибок при редактировании/удалении

### Нефункциональные требования
- [ ] Плавные анимации при переходах
- [ ] Responsive дизайн для мобильных устройств
- [ ] Быстрый отклик UI (оптимистичные обновления)
- [ ] Доступность (keyboard navigation)

## Технические детали

### Компоненты
- **TaskCard.tsx** - добавить кнопки Edit/Delete
- **TaskEditForm.tsx** (новый) - форма редактирования
- **DeleteConfirmModal.tsx** (новый) - подтверждение удаления
- **taskStore.ts** - добавить методы updateTask, deleteTask

### API endpoints (Supabase)
- `UPDATE tasks SET ... WHERE id = ?` - обновление задачи
- `DELETE FROM tasks WHERE id = ?` - удаление задачи

### DB schema
Изменений не требуется - используем существующую таблицу `tasks`

### State Management (Zustand)
```typescript
interface TaskStore {
  // ... existing
  updateTask: (id: string, updates: Partial<Task>) => Promise<void>;
  deleteTask: (id: string) => Promise<void>;
  editingTaskId: string | null;
  setEditingTaskId: (id: string | null) => void;
}
```

### UI/UX Flow

#### Редактирование:
1. Пользователь нажимает "Edit" на задаче
2. Задача переходит в режим редактирования (inline или modal)
3. Пользователь изменяет данные
4. Нажимает "Save" или "Cancel"
5. Изменения сохраняются в БД и UI обновляется

#### Удаление:
1. Пользователь нажимает "Delete" на задаче
2. Появляется модальное окно с подтверждением
3. Пользователь подтверждает или отменяет
4. При подтверждении - задача удаляется из БД и UI

## Тестирование

### Unit тесты
- [ ] taskStore.updateTask - успешное обновление
- [ ] taskStore.updateTask - обработка ошибок
- [ ] taskStore.deleteTask - успешное удаление
- [ ] taskStore.deleteTask - обработка ошибок

### Integration тесты
- [ ] Редактирование задачи через UI
- [ ] Отмена редактирования
- [ ] Удаление задачи через UI
- [ ] Отмена удаления

### E2E сценарии
- [ ] Полный цикл: создание → редактирование → сохранение
- [ ] Полный цикл: создание → удаление → подтверждение
- [ ] Редактирование нескольких задач подряд
- [ ] Проверка persistence после перезагрузки страницы

## Дизайн

### Кнопки на TaskCard
```
[Task Title]               [✏️ Edit] [🗑️ Delete]
Task description...
Jan 6, 2026, 02:46 PM
```

### Inline редактирование (предпочтительно)
```
[Input: Task Title]        [✓ Save] [✕ Cancel]
[Textarea: Description]
```

### Модальное окно удаления
```
┌────────────────────────────────┐
│ Delete Task?                   │
│                                │
│ Are you sure you want to       │
│ delete "Task Title"?           │
│ This action cannot be undone.  │
│                                │
│     [Cancel]  [Delete]         │
└────────────────────────────────┘
```

## Timeline

### Разработка: 2-3 часа
- TaskStore методы: 30 мин
- TaskCard кнопки: 30 мин
- TaskEditForm компонент: 1 час
- DeleteConfirmModal: 30 мин
- Интеграция и полировка: 30 мин

### Тестирование: 1 час
- Unit тесты: 20 мин
- Ручное тестирование: 20 мин
- E2E тесты: 20 мин

### Итого: 3-4 часа

## Риски и митигация

### Риск: Потеря данных при случайном удалении
**Митигация:** Модальное окно с подтверждением

### Риск: Конфликты при одновременном редактировании
**Митигация:** Используем updated_at timestamp для optimistic locking (будущая фича)

### Риск: Проблемы с производительностью при большом количестве задач
**Митигация:** Пока не актуально, но можно добавить pagination позже

## Зависимости
- Нет внешних зависимостей
- Используем существующие библиотеки (framer-motion, zustand)

## Последующие улучшения (не в этой фиче)
- История изменений задачи
- Мультиселект для массового удаления
- Drag & drop для изменения порядка
- Фильтрация и сортировка
